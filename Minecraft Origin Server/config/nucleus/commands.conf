afk {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        away=true
        nafk=false
    }
    # 设置此指令是否启用。
    enabled=true
}
afkkick {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        kickafk=true
        nafkkick=false
    }
    # 设置此指令是否启用。
    enabled=true
}
afkrefresh {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nafkrefresh=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
anvil {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nanvil=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
back {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nback=false
        return=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
ban {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nban=false
    }
    # 设置此指令是否启用。
    enabled=true
}
blockinfo {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nblockinfo=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
blockzap {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nblockzap=false
        zapblock=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
broadcast {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        bc=true
        bcast=true
        nbroadcast=false
    }
    # 设置此指令是否启用。
    enabled=true
}
checkban {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ncheckban=false
    }
    # 设置此指令是否启用。
    enabled=true
}
checkjail {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ncheckjail=false
    }
    # 设置此指令是否启用。
    enabled=true
}
checkjailed {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ncheckjailed=false
    }
    # 设置此指令是否启用。
    enabled=true
}
checkmute {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ncheckmute=false
    }
    # 设置此指令是否启用。
    enabled=true
}
checkmuted {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ncheckmuted=false
    }
    # 设置此指令是否启用。
    enabled=true
}
checknotes {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nchecknotes=false
        notes=false
    }
    # 设置此指令是否启用。
    enabled=true
}
checkwarnings {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        checkwarn=true
        ncheckwarnings=false
        warnings=true
    }
    # 设置此指令是否启用。
    enabled=true
}
clear {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ci=true
        clearinv=true
        clearinvent=true
        clearinventory=true
        nclear=false
    }
    # 设置此指令是否启用。
    enabled=true
}
clearnotes {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nclearnotes=false
        removeallnotes=true
    }
    # 设置此指令是否启用。
    enabled=true
}
clearwarnings {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nclearwarnings=false
        removeallwarnings=true
    }
    # 设置此指令是否启用。
    enabled=true
}
commandinfo {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ncommandinfo=false
    }
    # 设置此指令是否启用。
    enabled=true
}
commandspy {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ncommandspy=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
delnick {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        deletenick=true
        delnickname=true
        ndelnick=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
enchant {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nenchant=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
enchantingtable {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        enchanttable=true
        etable=true
        nenchantingtable=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
enderchest {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ec=true
        echest=true
        nenderchest=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
entityinfo {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nentityinfo=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
exp {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        experience=true
        nexp=false
        xp=true
    }
    # 设置此指令是否启用。
    enabled=true
}
extinguish {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ext=true
        nextinguish=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
feed {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        eat=true
        nfeed=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
firstspawn {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nfirstspawn=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
fly {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nfly=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
freezeplayer {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        freeze=true
        nfreezeplayer=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
gamemode {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        gm=true
        ngamemode=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
getfromip {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ngetfromip=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
getpos {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        coords=true
        getloc=true
        getlocation=true
        ngetpos=false
        position=true
        whereami=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
globalmute {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nglobalmute=false
    }
    # 设置此指令是否启用。
    enabled=true
}
gma {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        adventure=true
        ngma=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
gmc {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        creative=true
        ngmc=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
gms {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ngms=false
        survival=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
gmsp {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ngmsp=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
gmt {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ngmt=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
god {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        invuln=true
        invulnerability=true
        ngod=false
    }
    # 设置此指令是否启用。
    enabled=true
}
hat {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        head=true
        nhat=false
    }
    # 设置此指令是否启用。
    enabled=true
}
heal {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nheal=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
helpop {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nhelpop=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
home {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nhome=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"home.delete" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        deletehome=true
        delhome=true
    }
}
"home.deleteother" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        deletehomeother=true
        delhomeother=true
    }
}
"home.list" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        homes=true
        listhomes=true
    }
}
"home.other" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        homeother=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"home.set" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        homeset=true
        sethome=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
ignite {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        burn=true
        nignite=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
ignore {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nignore=false
    }
    # 设置此指令是否启用。
    enabled=true
}
ignorelist {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ignored=true
        listignore=true
        nignorelist=false
    }
    # 设置此指令是否启用。
    enabled=true
}
info {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        einfo=true
        ninfo=false
    }
    # 设置此指令是否启用。
    enabled=true
}
invsee {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ninvsee=false
    }
    # 设置此指令是否启用。
    enabled=true
}
itembuy {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        buy=true
        nitembuy=false
    }
    # 设置此指令是否启用。
    enabled=true
}
iteminfo {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        itemdb=true
        niteminfo=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
itemname {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nitemname=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"itemname.clear" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        clearitemname=true
        resetitemname=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"itemname.set" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        renameitem=true
        setitemname=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
itemsell {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nitemsell=false
        sell=true
    }
    # 设置此指令是否启用。
    enabled=true
}
itemsellall {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nitemsellall=false
        sellall=true
    }
    # 设置此指令是否启用。
    enabled=true
}
jail {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        njail=false
        togglejail=true
        unjail=true
    }
    # 设置此指令是否启用。
    enabled=true
}
jails {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        njails=false
    }
    # 设置此指令是否启用。
    enabled=true
}
"jails.delete" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        deletejail=true
        deljail=true
        rmjail=true
    }
}
"jails.set" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        createjail=true
        setjail=true
    }
}
jump {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        j=true
        jmp=true
        njump=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
kick {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nkick=false
    }
    # 设置此指令是否启用。
    enabled=true
}
kickall {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nkickall=false
    }
    # 设置此指令是否启用。
    enabled=true
}
kill {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nkill=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
killentity {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nkillentity=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
kit {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nkit=false
    }
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"kit.give" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"kit.list" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        kits=true
    }
}
kittycannon {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        kc=true
        nkittycannon=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
lightning {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nlightning=false
        shock=true
        smite=true
        strike=true
        thor=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
list {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        listplayers=true
        ls=true
        nlist=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
lockweather {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        killweather=true
        nlockweather=false
    }
    # 设置此指令是否启用。
    enabled=true
}
lore {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nlore=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"lore.add" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"lore.clear" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"lore.delete" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"lore.edit" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"lore.insert" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"lore.set" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
mail {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        email=true
        nmail=false
    }
    # 设置此指令是否启用。
    enabled=true
}
"mail.send" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        sendmail=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
me {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        action=true
        nme=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
message {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        m=true
        msg=true
        nmessage=false
        nreply=false
        r=true
        t=true
        tell=true
        w=true
        whisper=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
more {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nmore=false
        stack=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
motd {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nmotd=false
    }
    # 设置此指令是否启用。
    enabled=true
}
msgtoggle {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        messagetoggle=true
        mtoggle=true
        nmsgtoggle=false
    }
    # 设置此指令是否启用。
    enabled=true
}
mute {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nmute=false
        unmute=true
    }
    # 设置此指令是否启用。
    enabled=true
}
nameban {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nnameban=false
    }
    # 设置此指令是否启用。
    enabled=true
}
nameunban {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        namepardon=false
        nnameunban=false
    }
    # 设置此指令是否启用。
    enabled=true
}
nick {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nickname=false
        nnick=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
note {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        addnote=true
        nnote=false
    }
    # 设置此指令是否启用。
    enabled=true
}
nucleus {
    # 设置此指令是否启用。
    enabled=true
}
"nucleus.debug" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"nucleus.debug.setsession" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
nuserprefs {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        userprefs=true
    }
    # 设置此指令是否启用。
    enabled=true
}
ping {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nping=false
    }
    # 设置此指令是否启用。
    enabled=true
}
plainbroadcast {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nplainbroadcast=false
        pbc=true
        pbcast=true
    }
    # 设置此指令是否启用。
    enabled=true
}
powertool {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        npowertool=false
        pt=true
    }
    # 设置此指令是否启用。
    enabled=true
}
realname {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nrealname=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
removenote {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        deletenote=true
        delnote=true
        nremovenote=false
    }
    # 设置此指令是否启用。
    enabled=true
}
removewarning {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        deletewarning=true
        delwarn=true
        nremovewarning=false
    }
    # 设置此指令是否启用。
    enabled=true
}
repair {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        mend=true
        nrepair=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
rocket {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nrocket=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
rtp {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nrtp=false
        randomteleport=true
        rteleport=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
rules {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nrules=false
    }
    # 设置此指令是否启用。
    enabled=true
}
seen {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        lookup=true
        nseen=false
        seenplayer=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
serverlist {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nserverlist=false
        sl=true
    }
    # 设置此指令是否启用。
    enabled=true
}
"serverlist.message" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
serverstat {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        gc=true
        nserverstat=false
        uptime=true
    }
    # 设置此指令是否启用。
    enabled=true
}
servertime {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nservertime=false
        realtime=true
    }
    # 设置此指令是否启用。
    enabled=true
}
setfirstspawn {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nsetfirstspawn=false
    }
    # 设置此指令是否启用。
    enabled=true
}
setspawn {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nsetspawn=false
    }
    # 设置此指令是否启用。
    enabled=true
}
setworth {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nsetworth=false
        setitemworth=true
    }
    # 设置此指令是否启用。
    enabled=true
}
showitemattributes {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nshowitemattributes=false
        showattributes=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
skull {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nskull=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
socialspy {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nsocialspy=false
    }
    # 设置此指令是否启用。
    enabled=true
}
spawn {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nspawn=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
spawnmob {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        mobspawn=true
        nspawnmob=false
        spawnentity=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
speed {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nspeed=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
staffchat {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        a=true
        nstaffchat=false
        sc=true
    }
    # 设置此指令是否启用。
    enabled=true
}
stop {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nstop=false
    }
    # 设置此指令是否启用。
    enabled=true
}
sudo {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nsudo=false
    }
    # 设置此指令是否启用。
    enabled=true
}
suicide {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nsuicide=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
teleport {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nteleport=false
        tp=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
tellplain {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntellplain=false
        plaintell=true
        ptell=true
    }
    # 设置此指令是否启用。
    enabled=true
}
tempban {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntempban=false
    }
    # 设置此指令是否启用。
    enabled=true
}
thru {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nthru=false
        through=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
time {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntime=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"time.set" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        settime=true
        timeset=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
toggleviewstaffchat {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntoggleviewstaffchat=false
        togglevsc=true
        vsc=true
    }
    # 设置此指令是否启用。
    enabled=true
}
top {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntop=false
        tosurface=true
        totop=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
tpa {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        call=true
        ntpa=false
        teleportask=true
        tpask=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
tpaall {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntpaall=false
        tpaskall=true
    }
    # 设置此指令是否启用。
    enabled=true
}
tpaccept {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntpaccept=false
        teleportaccept=true
        tpyes=true
    }
    # 设置此指令是否启用。
    enabled=true
}
tpahere {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntpahere=false
        teleportaskhere=true
        tpaskhere=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
tpall {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntpall=false
        tpallhere=true
    }
    # 设置此指令是否启用。
    enabled=true
}
tpdeny {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntpdeny=false
        teleportdeny=true
        tpno=true
    }
    # 设置此指令是否启用。
    enabled=true
}
tphere {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntphere=false
        tph=true
    }
    # 设置此指令是否启用。
    enabled=true
}
tppos {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntppos=false
    }
    # 设置此指令是否启用。
    enabled=true
}
tptoggle {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntptoggle=false
    }
    # 设置此指令是否启用。
    enabled=true
}
trash {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntrash=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
unban {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nunban=false
        pardon=true
    }
    # 设置此指令是否启用。
    enabled=true
}
unsignbook {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nunsignbook=false
        unsign=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
unstuck {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nunstuck=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
vanish {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nvanish=false
        v=true
    }
    # 设置此指令是否启用。
    enabled=true
}
vanishonlogin {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nvanishonlogin=false
        vonlogin=true
    }
    # 设置此指令是否启用。
    enabled=true
}
voice {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nvoice=false
    }
    # 设置此指令是否启用。
    enabled=true
}
warn {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        addwarning=true
        nwarn=false
        warning=true
    }
    # 设置此指令是否启用。
    enabled=true
}
warp {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nwarp=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"warp.category" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"warp.delete" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"warp.list" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        warps=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"warp.set" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        setwarp=true
        warpset=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
weather {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nweather=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
workbench {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        craft=true
        nworkbench=false
        wb=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
world {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nucleusworld=false
        nworld=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"world.border" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"world.border.gen" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"world.create" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"world.gamerule" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        gamerules=true
    }
}
"world.info" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"world.list" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        worlds=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"world.sethardcore" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"world.setkeepspawnloaded" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"world.setloadonstartup" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"world.setpvpenabled" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"world.setspawn" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"world.spawn" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"world.teleport" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
worth {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nworth=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
