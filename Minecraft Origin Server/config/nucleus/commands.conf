back {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nback=false
        return=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
ban {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nban=false
    }
    # 设置此指令是否启用。
    enabled=true
}
checkban {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ncheckban=false
    }
    # 设置此指令是否启用。
    enabled=true
}
commandinfo {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ncommandinfo=false
    }
    # 设置此指令是否启用。
    enabled=true
}
home {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nhome=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"home.delete" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        deletehome=true
        delhome=true
    }
}
"home.deleteother" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        deletehomeother=false
        delhomeother=false
    }
}
"home.list" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        homes=true
        listhomes=true
    }
}
"home.other" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        homeother=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"home.set" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        homeset=false
        sethome=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
nameban {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nnameban=false
    }
    # 设置此指令是否启用。
    enabled=true
}
nameunban {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        namepardon=false
        nnameunban=false
    }
    # 设置此指令是否启用。
    enabled=true
}
nucleus {
    # 设置此指令是否启用。
    enabled=false
}
"nucleus.debug" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"nucleus.debug.setsession" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
nuserprefs {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        userprefs=true
    }
    # 设置此指令是否启用。
    enabled=true
}
teleport {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nteleport=false
        tp=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=false
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
tempban {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntempban=false
    }
    # 设置此指令是否启用。
    enabled=true
}
tpa {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        call=false
        ntpa=false
        teleportask=false
        tpask=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
tpaall {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntpaall=false
        tpaskall=false
    }
    # 设置此指令是否启用。
    enabled=true
}
tpaccept {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntpaccept=false
        teleportaccept=false
        tpyes=false
    }
    # 设置此指令是否启用。
    enabled=true
}
tpahere {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntpahere=false
        teleportaskhere=false
        tpaskhere=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
tpall {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntpall=false
        tpallhere=false
    }
    # 设置此指令是否启用。
    enabled=false
}
tpdeny {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntpdeny=false
        teleportdeny=false
        tpno=false
    }
    # 设置此指令是否启用。
    enabled=true
}
tphere {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntphere=false
        tph=false
    }
    # 设置此指令是否启用。
    enabled=false
}
tppos {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntppos=false
    }
    # 设置此指令是否启用。
    enabled=true
}
tptoggle {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        ntptoggle=false
    }
    # 设置此指令是否启用。
    enabled=false
}
unban {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nunban=false
        pardon=false
    }
    # 设置此指令是否启用。
    enabled=true
}
warp {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        nwarp=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 设置此指令是否启用。
    enabled=true
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"warp.category" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"warp.delete" {
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"warp.list" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        warps=true
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
"warp.set" {
    # 下面的指令为相关指令的简写形式。设置为false以禁用他们。
    aliases {
        setwarp=false
        warpset=false
    }
    # 以秒为单位的倒计时，玩家两次执行此指令之间需要等待的冷却时间。设置为0以禁用此功能。
    cooldown=0
    # 执行指令所需要的游戏币消费，游戏币仅会在指令执行成功之后被扣除。设置为0以禁用此功能。
    cost=0
    # 以秒为单位的倒计时，玩家执行指令之前需要等待的指令预热倒计时（译者注：预热期间，玩家不能移动且不能执行其他指令，否则会打断预热，导致指令执行失败）。设置为0以禁用此功能。
    warmup=0
}
